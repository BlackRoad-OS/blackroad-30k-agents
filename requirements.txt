# BlackRoad 30k Agents - Python Dependencies

# Core dependencies (no external packages needed for basic functionality)
# The base agent uses only Python standard library

# Optional: Redis backend for production memory storage
redis>=4.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Development tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
